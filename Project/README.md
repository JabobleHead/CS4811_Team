To Start The application

Install the required Dependacies

pip install requests beautifulsoup4 
pip install flask


First:
Make sure ollam is running either the desktop app or in console

Second:
Start the mcp server by running 
python mcpServer.py

Thrid:
Run the chatbot
python chatbot.py
